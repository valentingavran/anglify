import { StepperNextDirective } from './stepper-next.directive';

describe('StepperNextDirective', () => {
  it('should create an instance', () => {
    const directive = StepperNextDirective;
    expect(directive).toBeTruthy();
  });
});
