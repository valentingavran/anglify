import { TrapFocusDirective } from './trap-focus.directive';

describe('TrapFocusDirective', () => {
  it('should create an instance', () => {
    const directive = TrapFocusDirective;
    expect(directive).toBeTruthy();
  });
});
