import { InteractionStateDirective } from './interaction-state.directive';

describe('InteractionStateDirective', () => {
  const directive = InteractionStateDirective;
  it('should create an instance', () => {
    expect(directive).toBeTruthy();
  });
});
