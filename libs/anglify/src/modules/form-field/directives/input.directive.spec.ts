import { InputDirective } from './input.directive';

describe('InputDirective', () => {
  const directive = InputDirective;
  it('should create an instance', () => {
    expect(directive).toBeTruthy();
  });
});
