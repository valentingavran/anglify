export type EntireListItemDescriptionSettings = {
  /**
   * Limits the number of lines.
   */
  lineClamp: number;
};

export type ListItemDescriptionSettings = Partial<EntireListItemDescriptionSettings>;
